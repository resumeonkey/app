# Local development — points to local FastAPI
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Production (Vercel) — set this in Vercel dashboard → Settings → Environment Variables
# Backend migrado a Google Cloud Run (scale-to-zero, free tier):
# NEXT_PUBLIC_API_URL=https://resume-adapter-api-752460669266.us-central1.run.app
